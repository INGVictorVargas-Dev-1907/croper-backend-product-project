# ===========================
# 🌐 Configuración del Servidor
# Puerto en el que se levantará el backend.
# Ejemplo: 3000 para desarrollo local
PORT=3000

# ===========================
# 🗄️ Base de Datos
# URI de conexión a MongoDB. Cambia "croper_fullstack" por el nombre de tu base.
# Si usas MongoDB Atlas, reemplaza por la URI de conexión que te dé el servicio.
MONGODB_URI=mongodb://localhost:27017/croper_fullstack

# ===========================
# 🔐 Autenticación (JWT)
# Clave secreta usada para firmar y verificar los JWT.
# Debe ser larga y segura (NO compartir ni subir a GitHub).
JWT_SECRET=tu_clave_secreta_aqui

# Tiempo de expiración de los JWT.
# Ejemplo: "1d" = 1 día, "2h" = 2 horas, "30m" = 30 minutos.
JWT_EXPIRES_IN=1d
